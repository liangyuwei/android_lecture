<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.zju.id21832004.liangyuwei">

    <!-- Should declare permission request here, before the <application> tag !!! -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--android:roundIcon="@mipmap/ic_launcher_round"-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".HelloActivity"
            android:label="liangyuwei"
            android:theme="@style/AppTheme.NoActionBar">

            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".CalcActivity"
            android:label="@string/calc_name">

            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".StatusActivity"
            android:label="@string/post_weibo_name">

            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/weibo_info_list">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FileWriteActivity"
            android:label="@string/file_test_name" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/menu_settings_name" />

        <service
            android:name=".UpdateService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".MusicActivity"
            android:label="@string/get_music_info" />


    </application>

</manifest>